import type { PresetConfig } from './presets.js'

export const ruleName = 'no-object'

export const presetConfigs = [] satisfies PresetConfig[]

export const initialText = `const a = {
  a: 1,
  b: 2,
};
`
