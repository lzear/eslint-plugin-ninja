import type { PresetConfig } from './presets'

export const ruleName = 'no-random'

export const presetConfigs = [] satisfies PresetConfig[]

export const initialText = `const randomNumber = Math.random()
`
